%YAML 1.2
---
id: csmim.obj.aircraft.flight.1
description: >
  Flight data includes information about the current mission. This data is
  generally provided by flight management system(s) and relayed to the CSMIM
  system. Data is expected to be sent to the CSMIM system at either the native
  bus rate that is received by the data relay functionality, or at a sub-
  sampled bus rate. The messages provided herein will have the MQTT retain flag
  set if the nominal message period is > 1 sec.

resources:
  - id: origin
    type: string
    mode: r
    description: Origin Airport, 4-character ICAO Code

  - id: destination
    type: string
    mode: r
    description: Destination Airport, 4-character ICAO Code

  - id: number
    type: string[]
    mode: r
    description: Flight number assigned by the airline operator, including
      operator prefix. This field may contain code share flight numbers if
      available.

  - id: phase
    type: enum
    mode: r
    description: Phase of flight according to ARINC 854 §3.2.10
    FIXME: With notes about A843 equivalent and associated description
    enum-values:
      - name: pre_flight_ground
        key: 1
        description: system is powered
        FIXME: Equivalent to A843 GATE, when gnd-speed <= 2 kts
      - name: taxi_out
        key: 2
        description: after 1st engine start
        FIXME: A843 TAXI, when ground speed >= 2 kts
      - name: takeoff
        key: 3
        description: after 1st engine take-off power
        FIXME: A843 TAKEOFF, when CAS >= 80 kts
      - name: climb
        key: 4
        description: after lift-off
        FIXME: A843 CLIMB, when vertical speed >= 100 f/min
      - name: cruise
        key: 5
        description: above 1500 ft
        FIXME: A843 CRUISE, when vert-speed <= 100 f/min
      - name: descent
        key: 6
        description: below 800 ft
        FIXME: A843 DESCENT, when vert-speed <= -100 f/min
      - name: touch_down
        key: 7
        description: wheels on ground
        FIXME: A843 no equivalent
      - name: taxi_in
        key: 8
        description: speed drops below 50 kts
        FIXME: A843 TAXI, when CAS <= 80 kts
      - name: post_flight_ground
        key: 9
        description: engines off
        FIXME: A843 GATE, when gnd-speed <= 2 kts

  - id: leg_start
    optional: true
    type: utc
    mode: r
    description: Time of the start of the current flight leg, value is updated
      when a new flight leg starts
