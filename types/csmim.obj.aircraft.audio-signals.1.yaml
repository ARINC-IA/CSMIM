%YAML 1.2
---

FIXME: >
  not yet linked in path folder.
  Requires harmonization of cabin zone layout information

id: csmim.obj.aircraft.audio-signals.1
description: >
  The audio signals type provides discrete values, sent as booleans, indicating
  the state of the different audio channels being used.

resources:
  - id: passenger_address_active
    type: bool
    mode: r
    description: Whether the PA Keyline is active

    FIXME: maybe v1/data/aircraft/cabin/announcement/<zone/room/area>/passenger_address_active

  - id: pre_recorded_audio_active
    optional: true
    type: bool
    mode: r
    description: Whether the PRAM Keyline is active

    FIXME: maybe v1/data/aircraft/cabin/announcement/<zone/room/area>/pre_recorded_audio_active
